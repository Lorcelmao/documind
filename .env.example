# Copy to .env and fill in real values. Compose passes JWT_SECRET to the api service.
JWT_SECRET=replace-with-a-long-random-string

# Only needed when running the api outside compose (db is published on host port 5433):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/documind
# REDIS_URL=redis://localhost:6379/0
